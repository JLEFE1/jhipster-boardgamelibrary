version: '2'
services:
    users-postgresql:
        image: postgres:9.5.4
        # volumes:
        #     - ~/volumes/jhipster/users/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=users
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
